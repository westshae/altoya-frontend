import styled from "styled-components";

const StyledFormDiv = styled.form`
`

//Component returned
//TODO Use props to take components to place inside
const FormDiv = () =>{
    return(
        <StyledFormDiv>
        </StyledFormDiv>
    )
}

export default FormDiv;